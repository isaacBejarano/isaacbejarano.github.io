{
  "name": "p3",
  "version": "3.0.0",
  "description": "HTML-CSS-P3",
  "source": [
    "*.html",
    "css/*",
    "img/*",
    "validations/*"
  ],
  "author": "Isaac Bejarano",
  "license": "ISC",
  "scripts": {
    "parcel:dev": "parcel",
    "parcel:dist": "parcel build --dist-dir dist --public-url ./ --no-content-hash --no-optimize --no-source-maps --no-cache",
    "clean": "rimraf dist docs .parcel-cache",
    "start": "npm-run-all clean parcel:dev",
    "build": "npm-run-all clean parcel:dist"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "parcel": "^2.13.3",
    "rimraf": "^6.0.1",
    "svgo": "^3.3.2"
  }
}
